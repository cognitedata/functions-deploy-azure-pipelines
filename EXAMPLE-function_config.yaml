# Note: Parameters can be hardcoded directly in 'azure-pipelines.yaml'
# if you do not need to vary them between your functions!
description: "This function does an important job. Yes :doge:"
owner: The Awesome Forge Team
cpu: 0.1
memory: 0.2
common_folder: common
# Parameters that are usually not needed to pass (they have good default values):
# function_deploy_timeout: 1200
# post_deploy_cleanup: true
# function_file: handler.py
# env_vars: '{"some_env_var": "foo", "ANOTHER_ENV_VAR": "bar"}'
# remove_only: false
